export const metadata = {
  title: "Full Stack CRUD Operations",
  description: "Full Stack CRUD Operations",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <script src="https://cdn.tailwindcss.com"></script>
      </head>
      <body>
        {children}
      </body>
    </html>
  );
}
